enum MelangeLocalSecrets {
    // Local fallback for development if the generated Info.plist key is not making it into
    // the app bundle. Leave empty in source control and set your real key locally.
    static let personalKey = "ztp_7c8dae5533ee422db0e42d8b52337741"
}
